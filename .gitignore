# gradle

.gradle/
build/
out/
classes/

# eclipse

*.launch

# idea

.idea/
*.iml
*.ipr
*.iws

# vscode

.settings/
.vscode/
bin/
.classpath
.project

# macos

*.DS_Store

# fabric

run/

# cloudflared / overlay secrets (never commit)
**/cloudflared.exe
C:/Cloudflared/
tools/overlay-setup/*.token
tools/overlay-setup/*credentials*
tools/overlay-setup/*.json
*.tunnel-token
**/overlay-public-config.local.*

# local HTTPS overlay runtime (private keys / PFX / DPAPI secrets — never commit)
**/overlay-https/*.p12
**/overlay-https/*.pfx
**/overlay-https/*.key
**/overlay-https/*.pem
**/overlay-https/*.dpapi
**/overlay-https/*password*
**/overlay-https/*secret*
**/overlay-https/*.pass*
%LOCALAPPDATA%/ArenaOfNations/
ArenaOfNations/overlay-https/

# java

hs_err_*.log
replay_*.log
*.hprof
*.jfr

# local minecraft/server logs (never commit)
logs/
